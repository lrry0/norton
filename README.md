# Norton Support Portal Clone

A pixel-perfect clone of the Norton Support homepage with interactive simulated troubleshooting wizards, scanner systems, and a fully responsive layout.

## Run Locally

**Prerequisites:** Node.js, pnpm/npm

1. Install dependencies:
   `pnpm install` or `npm install`
2. Run the application:
   `pnpm run dev` or `npm run dev`
